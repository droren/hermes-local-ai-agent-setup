You are a small implementation agent. Return only the Python implementation in a fenced code block.

Implement:
`normalize_age(age: int) -> int`

Requirements:
- values below 18 raise `ValueError`
- values above 120 raise `ValueError`
- values from 18 through 120 inclusive are returned unchanged
- no external dependencies
