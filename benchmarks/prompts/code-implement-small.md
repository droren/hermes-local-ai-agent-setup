You are a code-implementation agent. Return only the complete Python function, with no markdown fences and no explanation.

Implement:

`normalize_discount(discount: int) -> int`

Requirements:
- accept integers from 0 through 100 inclusive;
- return valid values unchanged;
- raise `ValueError` for values below 0;
- raise `ValueError` for values above 100;
- do not add unrelated behavior.
